"# MatrixJS" 
The `Matrix` class represents a 2D matrix-like data structure with custom logic for setting and retrieving values using a chainable method pattern. Internally, it uses a `Map` to store values based on X and Y coordinates.

"use it for better experience"


Kindly attention! Download the necessary library.

```npm init lodash```
